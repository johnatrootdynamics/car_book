FROM docker:27-cli

RUN apk add --no-cache python3 py3-pip git bash

WORKDIR /repo

RUN pip install --break-system-packages Flask==3.1.0

EXPOSE 9000

CMD ["python3", "/repo/deploy/webhook_server.py"]
